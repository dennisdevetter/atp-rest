var api = require('../../src/api').default;
var config = require('../../src/config').default;

describe('The API REST server',() => {    
  it('should start', () => {                          
  		// how to test this?!  	 
  });
});  